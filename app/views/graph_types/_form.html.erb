<div class="row">
<div class="col-md-5">
<div class="form">

<%= form_for(@graph_type) do |f| %>
  <% if @graph_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@graph_type.errors.count, "error") %> prohibited this graph_type from being saved:</h2>

      <ul>
      <% @graph_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group">
    <%= f.label :name %><br>
    <%= f.text_field :name, class: "form-control" %>
  </div>
  <div class="actions">
    <%= f.submit class: "btn btn-default" %>
  </div>
<% end %>
