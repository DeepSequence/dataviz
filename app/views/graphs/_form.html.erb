<div class="row">
<div class="col-md-5">
<div clas="form">
<%= form_for(@graph) do |f| %>
  <% if @graph.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@graph.errors.count, "error") %> prohibited this graph from being saved:</h2>

      <ul>
      <% @graph.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field form-group">
    <%= f.label :name %><br>
    <%= f.text_field :name, class: "form-control" %>
  </div>
  <div class="field form-group">
    <%= f.label :dataset_id %><br>
    <%= f.collection_select :dataset_id, Dataset.all, :id, :supported_graph_types, {include_blank: "Select Dataset..."},{ class: "form-control"} %>
  </div>
  <div class="field form-group">
    <%= f.label :graph_type_id %><br>
    <%= f.collection_select :graph_type_id, GraphType.all, :id, :name, {include_blank: "Select Graph Type..."}, {class: "form-control"} %>
  </div>
  <div class="field form-group">
    <%= f.label :x_axis_label %><br>
    <%= f.text_field :x_axis_label, class: "form-control" %>
  </div>
  <div class="field form-group">
    <%= f.label :y_axis_label%><br>
    <%= f.text_field :y_axis_label, class: "form-control" %>
  </div>

  <div class="actions">
    <%= f.submit class: "btn btn-default" %>
  </div>
<% end %>
</div>
</div>
<div class="col-md-1"></div>
<div class="col-md-6">
<div id="new-graph-instructions">
<h2> How to create your own graph from a downloaded dataset </h2>
<ul class= "list-unstyled step-by-step">
<li> <strong>Step 1: </strong> After creating your custom dataset and saving on your computer, select your dataset from the dropdown list on the left (the graph type that works with that particular dataset type is listed for you, in parentheses). </li><br/>
<li> <strong>Step 2: </strong> Select the graph type. It's important to choose the right graph type here, as some dataset types do not work with some graph types. </li><br/>
<li><strong>Step 3: </strong>Label the x axis and the y axis.</li><br/>
<li><strong> Step 4: </strong>Click on the "Create Graph" to create your brand new graph! You can then share your new graph on Twitter.</li>
</ul>
</div>
